import React from 'react';
import InCLeads from "../assets/images/incoming-leads.png";
const InLeads = () => {
    return (
        <div>
            <div className="in-coming-leads">
                <img className='img-fluid' src={InCLeads} alt="in-coming-leads" />
            </div>
        </div>
    );
};

export default InLeads;